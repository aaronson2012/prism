## Required
DISCORD_TOKEN=your_discord_token_here
OPENROUTER_API_KEY=your_openrouter_key_here

## Models
DEFAULT_MODEL=google/gemini-2.5-flash
# FALLBACK_MODEL=google/gemini-2.5-flash-lite
# Optional: use a specific model for guild-wide backfill runs
# e.g., use Gemini 2.5 Flash for backfilling
# BACKFILL_MODEL=google/gemini-2.5-flash

## Backfill performance tuning (optional)
# Prism now pre-filters messages and batches DB writes to speed up backfills.
# You can further tune throughput with these knobs:
# - Increase to fetch more messages per API call (Discord API paginates internally; values >100 are fine)
# BACKFILL_BATCH_SIZE=200
# - Sleep between batches in seconds (use 0 for fastest)
# BACKFILL_SLEEP_S=0
# - Number of channels to scan concurrently during guild-wide backfill (1–4 is typical on a single host)
# BACKFILL_CHANNEL_CONCURRENCY=2
# - Number of LLM extractions to run in parallel within a channel
#   Be mindful of cost and provider rate limits (4–8 is common).
# BACKFILL_MESSAGE_CONCURRENCY=6

## OpenRouter app metadata (optional)
# OPENROUTER_SITE_URL=https://your.site/
# OPENROUTER_APP_NAME=Prism Bot

## Database & logging (optional)
# PRISM_DB_PATH=data/prism.db
# LOG_LEVEL=INFO

## Discord intents (optional)
# INTENTS_MESSAGE_CONTENT=true

## Feature toggles (optional)
# LEARNING_ENABLED=true
# EMOJI_TALK_ENABLED=true
# EMOJI_REACTIONS_ENABLED=false

## Fast guild slash command sync (optional)
# COMMAND_GUILD_IDS=691837132981141615,1415186232404742177
# Optional: fast guild slash command sync (comma-separated guild IDs)
# COMMAND_GUILD_IDS=691837132981141615,1415186232404742177
